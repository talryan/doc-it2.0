module UsersHelper

    #if user's upcomming appointments = 0, return "You have no upcomming appointments"
else 
    #if user's upcoming appointments > 0, return "You have #{number of upcoming appointments} and list upcoming apppointments"
end
