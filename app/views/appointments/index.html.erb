<center>
<h1> Appointment History</h1>
<h4><%=appointment_history?%></h4>
<% @appointments.each do |appointment| %> 
<%= link_to "#{appointment.date&.strftime("%B %e, %Y")}", appointment_path(appointment)%><br>
<%end%>
</center>