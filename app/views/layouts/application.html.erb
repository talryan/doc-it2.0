<!DOCTYPE html>
<html>
  <head>
    <title>Doc on Dec</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

   <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <% if logged_in? %>
      <a class="navbar-brand" href="<%= user_path(current_user)%>"/>Home</a>
     
      <a class="navbar-brand" href="<%= appointments_path %>"/>Appointment History</a>
      <a class="navbar-brand" <%= link_to "Log Out", logout_path, method: 'delete' %></a>

       <% else%>
       <a class="navbar-brand" href="<%= root_path%>"/>Home</a>
      <a class="navbar-brand" href="<%= '/login' %>"/>Sign In</a>
      <a class="navbar-brand" href="<%= new_user_path %>"/>Sign Up</a>
        <%end%>
    </div>
  </head>

  <body>
<br> <br> <br> <br>
		<% flash.each do |name, msg| %>
			<%= content_tag(:div, msg, class: "alert alert-info") %>
		<% end %>
    <%= yield %>
     <footer class="branding">
        <small>&copy; 2021 <strong>DocOnDec</strong></small>
      </footer><br>
     
  </body>
</html>
